{
  "name": "live-mtg-desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "build:backend": "node scripts/build-backend.mjs",
    "icons": "tauri icon icon.svg",
    "dev": "npm run build:backend && tauri dev",
    "build": "npm run build:backend && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.5"
  }
}
